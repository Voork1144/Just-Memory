# Just-Memory Environment Variables
# All optional. Copy to .env or set in your Claude Desktop config "env" block.

# Embedding model size: "large" (1024-dim e5-large-v2) or "small" (384-dim e5-small-v2)
# JUST_MEMORY_EMBEDDING=large

# Summarization model for chat ingestion
# JUST_MEMORY_SUMMARIZER=Xenova/distilbart-cnn-12-6

# Enable Qdrant vector DB for 1M+ memory scale. Set "false" to use sqlite-vec only
# JUST_MEMORY_QDRANT=true

# Qdrant gRPC port
# JUST_MEMORY_QDRANT_PORT=6333

# Path to Qdrant binary (auto-downloaded if missing)
# JUST_MEMORY_QDRANT_BINARY=~/.just-memory/qdrant/bin/qdrant

# Max concurrent SQLite writers (WAL mode)
# JUST_MEMORY_MAX_WRITERS=1

# Default project ID (also reads CLAUDE_PROJECT)
# JUST_MEMORY_PROJECT=
